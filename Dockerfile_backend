# Backend Dockerfile
FROM debian:latest

# Install PHP and MariaDB
RUN apt-get update && apt-get install -y php mariadb-server

# Copy backend files
COPY backend/ /var/www/html/

EXPOSE 80

# Start Apache
CMD ["apache2ctl", "-D", "FOREGROUND"]
